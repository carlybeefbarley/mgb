import React, { PropTypes } from 'react'

// MapActorGameEngine GameCanvas
// This handles just rendering the current game frame based on data available to it...

export default class MageGameCanvas {

  render () {
    return <canvas />
  }
}