{
    "compilerOptions": {
        "target": "ES6",
        "allowSyntheticDefaultImports": true

    },
    "exclude": [
        "./.meteor",
        "./typings/main.d.ts",
        "./typings/main",
        "./node_modules",
        "./public"            // Lots of doc files in here. Need to subdivide more
    ]
}